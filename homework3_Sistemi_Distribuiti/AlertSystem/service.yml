apiVersion: v1
kind: Service
metadata:
  name: alert-system
spec:
  selector:
    app: alert-system
  ports:
    - protocol: TCP
      port: 50051  
      targetPort: 50051
      nodePort: 30002  # Porta per le metriche Prometheus
  type: NodePort
