apiVersion: v1
kind: Service
metadata:
  name: datacollector
spec:
  selector:
    app: datacollector
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051
      nodePort: 30051
  type: NodePort